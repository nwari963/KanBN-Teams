[package]
name = "otsical-desktop"
version = "0.1.0"
edition = "2021"
description = "Thin desktop wrapper for the self-hosted OTSICAL instance"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
